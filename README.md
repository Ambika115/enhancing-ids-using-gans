# IDS using GANs

This project detects network attacks using GAN-based intrusion detection.

## Steps to Run

1 Install requirements
pip install -r requirements.txt

2 Train the model
python src/train_ids.py

3 Run the dashboard
streamlit run dashboard/app.py